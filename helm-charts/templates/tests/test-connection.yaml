apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "docker-hits.fullname" . }}-test-connection"
  labels:
    {{- include "docker-hits.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "docker-hits.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
